import { sql } from "../../../../db";
import { errorResponse,requireAdmin,writeAudit } from "../../../../db/admin-auth";
const seed=[["atlas-ui-kit","Atlas UI System","A meticulous Figma system with 640+ responsive components.","Design system",7900,"3.2.0"],["focusflow","FocusFlow","A calm focus timer with intelligent session planning.","Software",3900,"2.1.0"],["launch-playbook","The Launch Playbook","A field-tested system for shipping a digital product.","Ebook",2900,"1.4.0"]];
async function seedIfEmpty(){const count=await sql<{value:number}[]>`select count(*)::int as value from products`;if((count[0]?.value??0)>0)return;for(const p of seed)await sql`insert into products(slug,name,description,product_type,status,price_cents,currency,version,download_limit) values (${String(p[0])},${String(p[1])},${String(p[2])},${String(p[3])},'published',${Number(p[4])},'usd',${String(p[5])},5) on conflict(slug) do nothing`}
export async function GET(){try{await requireAdmin("products");await seedIfEmpty();const products=await sql`select id,slug,name,description,product_type as "productType",status,price_cents as "priceCents",currency,version,download_limit as "downloadLimit",updated_at as "updatedAt" from products order by updated_at desc`;return Response.json({products})}catch(error){return errorResponse(error)}}
export async function POST(request:Request){try{const admin=await requireAdmin("products");const body=await request.json() as Record<string,unknown>;const name=String(body.name??"").trim(),slug=String(body.slug??"").trim().toLowerCase(),description=String(body.description??"").trim(),productType=String(body.productType??"").trim(),version=String(body.version??"1.0.0").trim(),priceCents=Math.round(Number(body.price)*100);if(name.length<2||!/^[a-z0-9]+(?:-[a-z0-9]+)*$/.test(slug)||description.length<10||!productType||!Number.isInteger(priceCents)||priceCents<0)return Response.json({error:"Enter valid product information."},{status:400});const rows=await sql<{id:string}[]>`insert into products(slug,name,description,product_type,status,price_cents,currency,version,download_limit) values (${slug},${name},${description},${productType},'draft',${priceCents},'usd',${version},5) returning id`;await writeAudit(admin,"product.create","product",rows[0].id,{name,slug});return Response.json({id:rows[0].id},{status:201})}catch(error){return errorResponse(error)}}
